name: CI

on:
  push:
    branches: [ main ]

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Dependencies
        run: npm clean-install

      - name: Publish
        run: npm run publish
