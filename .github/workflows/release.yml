name: CI

on:
  push:
    branches: [ main ]

jobs:
  release:
    runs-on: windows-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3

      - name: dependencies
        run: npm install

      - name: make
        run: npm run make
